name: import.meta.url.split('/').pop()
input: { x, y, z, mass, time, axiom }
set(input) { Object.assign(this.input, input) }
compute() {
    sum = x+y+z
    torque = sum * mass * time
}
axiomVector(key)
